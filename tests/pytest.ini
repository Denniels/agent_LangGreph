[tool:pytest]
# Configuración de pytest para el proyecto

# Directorio de tests
testpaths = tests

# Patrones de archivos de test
python_files = test_*.py *_test.py

# Patrones de clases de test
python_classes = Test*

# Patrones de funciones de test
python_functions = test_*

# Opciones por defecto
addopts = 
    -v
    --tb=short
    --strict-markers
    --strict-config
    --disable-warnings

# Marcadores personalizados
markers =
    unit: Marca tests unitarios
    integration: Marca tests de integración
    slow: Marca tests lentos
    database: Marca tests que requieren base de datos
    agent: Marca tests del agente conversacional
    tools: Marca tests de herramientas

# Filtros de warnings
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

# Configuración de cobertura (si se usa pytest-cov)
# addopts = --cov=modules --cov-report=html --cov-report=term-missing

# Configuración asyncio
asyncio_mode = auto
